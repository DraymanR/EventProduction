'use client'

import PostList from "./component/posts/PostList";

const Home = () => {
  // console.log(decodeURIComponent(document.cookie));

  return (
    <div>
      <PostList />
    </div>
)}


export default Home;
